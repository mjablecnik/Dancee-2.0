name: dancee_event_service
description: A server app using the shelf package and Docker.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.4

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  
  # Shared models and utilities
  dancee_shared:
    path: ../../shared/dancee_shared

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
