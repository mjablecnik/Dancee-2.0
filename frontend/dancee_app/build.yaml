# Build configuration for code generation
# Used by build_runner for go_router_builder, freezed, json_serializable, and slang
targets:
  $default:
    builders:
      # go_router_builder configuration
      go_router_builder:
        enabled: true
      # freezed configuration
      freezed:
        enabled: true
      # json_serializable configuration
      json_serializable:
        enabled: true
        options:
          explicit_to_json: true
      # slang translation generation
      slang_build_runner:
        enabled: true
